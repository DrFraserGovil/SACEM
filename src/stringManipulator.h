#ifndef STRINGMANIPULATOR
#define STRINGMANIPULATOR
#include <fstream>
#include <sstream>
#include <string>
#include <vector>

using namespace std;
std::vector<string> split(const string& s, char delimiter);
#endif

